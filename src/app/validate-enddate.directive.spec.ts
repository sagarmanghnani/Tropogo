import { ValidateEnddateDirective } from './validate-enddate.directive';

describe('ValidateEnddateDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateEnddateDirective();
    expect(directive).toBeTruthy();
  });
});
