import { PositivecostDirective } from './positivecost.directive';

describe('PositivecostDirective', () => {
  it('should create an instance', () => {
    const directive = new PositivecostDirective();
    expect(directive).toBeTruthy();
  });
});
